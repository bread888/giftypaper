---
// FormattedDate.astro
interface Props {
	date: Date;
}
const { date } = Astro.props;
---
<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('it-it', { // Cambiato in it-it
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>